interface:
  display_name: "aiwf_avoid-ai-pushes"
  short_description: "Verify commit and push scope"
  default_prompt: "Use $aiwf-avoid-ai-pushes to audit this repo before committing or pushing."
policy:
  allow_implicit_invocation: false
