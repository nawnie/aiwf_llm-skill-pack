interface:
  display_name: "aiwf_nvidia-cuda-cudnn-sdk"
  short_description: "CUDA, cuDNN, TensorRT, and SDK guardrails"
  default_prompt: "Use $aiwf-nvidia-cuda-cudnn-sdk for CUDA, cuDNN, TensorRT, SDK Manager, NVIDIA SDK, nvcc, and GPU kernel work."
policy:
  allow_implicit_invocation: false
