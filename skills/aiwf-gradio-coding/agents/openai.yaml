interface:
  display_name: "aiwf_gradio-coding"
  short_description: "Gradio Blocks, Interface, events, and mounts"
  default_prompt: "Use $aiwf-gradio-coding for Gradio Blocks, Interface, component events, queueing, FastAPI mounting, auth, and UI smoke tests."
policy:
  allow_implicit_invocation: false
