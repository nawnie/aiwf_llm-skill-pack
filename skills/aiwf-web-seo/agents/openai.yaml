interface:
  display_name: "aiwf_web-seo"
  short_description: "Technical SEO, rendering, indexing, and validation"
  default_prompt: "Use $aiwf-web-seo to audit HTTP behavior, crawlability, rendering, metadata, canonical URLs, sitemaps, structured data, performance, and production indexability."
policy:
  allow_implicit_invocation: false
