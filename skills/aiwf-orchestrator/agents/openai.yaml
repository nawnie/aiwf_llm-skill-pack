interface:
  display_name: "aiwf_orchestrator"
  short_description: "Route AIWF prompts to the right skill"
  default_prompt: "Use $aiwf-orchestrator to infer the right AIWF skill route from this prompt before doing non-trivial local AI work."
policy:
  allow_implicit_invocation: true
